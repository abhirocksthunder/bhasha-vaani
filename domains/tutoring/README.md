# Tutoring Domain

The tutoring engine orchestrates lessons using profile settings, language pack metadata, progress projections, and provider gateway output.

It must not contain language-specific branches such as `if language == "kn"`.

Language-specific content belongs in `language_packs`.
